# Portfolio - apolyakov.tech


## Возможности

- **AI Чат-ассистент** - Интеллектуальный ассистент на базе Groq AI с Gemini в качестве резервного провайдера
- **Telegram уведомления в реальном времени** - Получайте уведомления о взаимодействии пользователей с AI ассистентом
- **Адаптивный дизайн** - Mobile-first дизайн с плавными анимациями
- **Многоязычность** - Поддержка английского и русского языков
- **База данных PostgreSQL** - Постоянное хранилище для истории чатов и аналитики
- **RESTful API** - Версионированные API эндпоинты с OpenAPI документацией
- **Docker Ready** - Мультиконтейнерное развёртывание с оркестрацией

## Быстрый старт (Docker)

```bash
# 1. Создайте .env файл (см. раздел Переменные окружения)
cp .env.example .env
# Отредактируйте .env и добавьте ваши API ключи

# 2. Запустите все сервисы
docker-compose up -d --build

# 3. Откройте приложение
# Frontend: http://localhost:3000
# Backend API: http://localhost:8000
# API Docs: http://localhost:8000/docs
```

**Сервисы:**
- `postgres` - База данных PostgreSQL 16
- `backend` - FastAPI приложение (порт 8000)
- `frontend` - Next.js приложение (порт 3000)


## Переменные окружения

Создайте `.env` файл в корне проекта (используйте `.env.example` как шаблон):

```env
# База данных (PostgreSQL)
DATABASE_URL=postgresql://portfolio_user:portfolio_password@postgres:5432/portfolio_db
POSTGRES_USER=portfolio_user
POSTGRES_PASSWORD=portfolio_password
POSTGRES_DB=portfolio_db
POSTGRES_PORT=5432

# AI API ключи (обязательно)
GROK_API_KEY=your_groq_api_key_here          # Основной AI провайдер
GEMINI_API_KEY=your_gemini_api_key_here      # Резервный AI провайдер

# Telegram уведомления (опционально)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHI...    # Токен бота от @BotFather
TELEGRAM_CHAT_ID=your_chat_id                # Ваш Telegram chat ID

# URLs
BACKEND_URL=http://localhost:8000            # URL бэкенда для фронтенда
```

### Получение API ключей

**Groq API (Основной):**
1. Перейдите на [console.groq.com](https://console.groq.com)
2. Создайте аккаунт и сгенерируйте API ключ
3. Добавьте в `GROK_API_KEY` в `.env`

**Gemini API (Резервный):**
1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создайте API ключ
3. Добавьте в `GEMINI_API_KEY` в `.env`

**Telegram Bot (Опционально):**
1. Напишите [@BotFather](https://t.me/BotFather) → `/newbot`
2. Скопируйте токен в `TELEGRAM_BOT_TOKEN`
3. Получите ваш chat ID от [@userinfobot](https://t.me/userinfobot)
4. Добавьте в `TELEGRAM_CHAT_ID`

## Структура проекта

```
portfolio_dreusus/
├── frontend/                # Next.js приложение
│   ├── src/
│   │   ├── app/            # Next.js App Router
│   │   ├── pages/          # Компоненты страниц
│   │   ├── widgets/        # Сложные UI компоненты (Header, Footer, ChatWidget)
│   │   ├── features/       # Модули функций (about, contact, projects, skills)
│   │   └── shared/         # Переиспользуемый код (ui, hooks, i18n, utils)
│   └── package.json
│
├── backend/                 # FastAPI приложение
│   ├── src/
│   │   ├── api/            # API эндпоинты (v1 + legacy)
│   │   ├── services/       # Бизнес-логика
│   │   ├── repositories/   # Слой доступа к данным
│   │   ├── models/         # SQLAlchemy модели
│   │   ├── schemas/        # Pydantic схемы
│   │   ├── core/           # Конфигурация
│   │   └── middleware/     # Логирование запросов
│   ├── alembic/            # Миграции базы данных
│   ├── pyproject.toml      # Python зависимости
│   └── main.py             # Точка входа приложения
│
├── docker-compose.yml       # Production оркестрация
├── .env.example            # Шаблон переменных окружения
└── README.md               # Этот файл
```

## API документация

Backend предоставляет версионированный API с автоматической документацией:

- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

### Эндпоинты

**Текущие (v1):**
- `GET /api/v1/chat/history` - Получить историю чата
- `POST /api/v1/chat` - Отправить сообщение AI
- `GET /api/v1/health` - Проверка здоровья

**Legacy (устаревшие):**
- `GET /request` - Старый эндпоинт истории чата
- `POST /request` - Старый эндпоинт чата

## Миграции базы данных

```bash
cd backend

# Создать новую миграцию
alembic revision --autogenerate -m "описание"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1

# Просмотр истории
alembic history
```

## Технологический стек

### Frontend
- **Next.js 15** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript** - Типобезопасность
- **Tailwind CSS 4** - Utility-first CSS
- **Framer Motion** - Анимации
- **Shadcn/ui** - Библиотека компонентов
- **React Hook Form + Zod** - Валидация форм
- **Formspree** - Бэкенд для контактной формы

### Backend
- **FastAPI** 0.126.0 - Современный async веб-фреймворк
- **SQLAlchemy** 2.0.45 - ORM с async поддержкой
- **PostgreSQL** 16 - Production база данных
- **Alembic** - Миграции базы данных
- **Pydantic** 2.12.5 - Валидация данных
- **Uvicorn** - ASGI сервер
- **Groq AI** - Основной LLM провайдер
- **Google Gemini** - Резервный LLM провайдер
- **Telegram Bot API** - Уведомления в реальном времени

### Инфраструктура
- **Docker** - Контейнеризация
- **Docker Compose** - Мультиконтейнерная оркестрация
- **pnpm** - Быстрый пакетный менеджер

## Архитектура

### Frontend (Feature-Sliced Design)
- **app/** - Роуты и layout'ы
- **pages/** - Компоненты уровня страниц
- **widgets/** - Сложные составные компоненты
- **features/** - Бизнес-функции
- **shared/** - Переиспользуемые утилиты

### Backend (Слоистая архитектура)
- **API Layer** - HTTP эндпоинты, валидация
- **Service Layer** - Бизнес-логика, оркестрация
- **Repository Layer** - Операции с базой данных
- **Model Layer** - ORM сущности

## Telegram интеграция

При настройке backend отправляет уведомления в реальном времени о взаимодействиях с AI чатом:

```
🤖 Новый запрос к AI ассистенту

👤 IP: 192.168.1.100
💬 Запрос:
Привет, расскажи о портфолио

🌐 Браузер: Chrome
```

Ошибки не блокируют работу - чат продолжает работать даже если Telegram недоступен.



## Документация

- **Backend README:** [backend/README.md](backend/README.md) - Детальная архитектура бэкенда
- **Frontend README:** [frontend/README.md](frontend/README.md) - Детальная архитектура фронтенда

## Лицензия

Приватный проект - Все права защищены
